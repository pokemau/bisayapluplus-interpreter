{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "build and compile",
			"type": "shell",
			"command": "cmake --build build",
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"problemMatcher": ["$gcc"],
			"detail": "Task to run cmake configuration and build"
		}
	]
}
